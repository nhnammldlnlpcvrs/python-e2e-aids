# Stage 1: Python Core

From basics to async — write idiomatic, performant Python.

## Folder Structure

```
01-python-core/
├── theory/       # Markdown notes by topic
├── code/         # Runnable Python examples
└── projects/     # Mini-projects for portfolio
```

## Topics

1. Foundations — types, mutability, memory model
2. Control Flow — structural pattern matching, error handling
3. Functions — args/kwargs, closures, partial application
4. Iterables — generators, `yield from`, `itertools`
5. Decorators — `@wraps`, factories, class decorators
6. Context Managers — `__enter__`/`__exit__`, `contextlib`
7. Concurrency — threading, multiprocessing, GIL
8. Asyncio — event loop, `async/await`, `aiohttp`
9. Tooling — `pyproject.toml`, `pytest`, `ruff`, `mypy`

## Key Math

Amdahl's Law: $S = \frac{1}{(1-p) + p/n}$

## Projects

1. CLI File Organizer
2. Async News Aggregator
